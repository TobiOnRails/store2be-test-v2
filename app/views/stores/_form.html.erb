<%= form_for(store) do |f| %>
  <% if store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %>
    <%= f.number_field :address %>
  </div>

  <div class="field">
    <%= f.label :organization %>
    <%= f.number_field :organization %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :opening %>
    <%= f.time_select :opening %>
  </div>

  <div class="field">
    <%= f.label :closing %>
    <%= f.time_select :closing %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
